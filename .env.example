# MongoDB Configuration
MONGODB_URI=mongodb+srv://username:password@cluster.mongodb.net/brighton-gym?retryWrites=true&w=majority
# Alternative MongoDB URI variable name (the app supports both)
MONGO_URI=mongodb+srv://username:password@cluster.mongodb.net/brighton-gym?retryWrites=true&w=majority

# Server Configuration
PORT=3000
NODE_ENV=production

# CORS Configuration (optional)
# ALLOWED_ORIGINS=http://localhost:3000,https://yourdomain.com

# Instructions:
# 1. Copy this file to .env
# 2. Replace the MongoDB URI with your actual MongoDB Atlas connection string
# 3. Update the username, password, and cluster details
# 4. Set NODE_ENV to 'development' for local development